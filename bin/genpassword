#!/usr/bin/env bash

# Generate strong password

main() {
  file=$( mktemp );
  for i in 1 2 3 4 5 6 7 8; do
    [ $i != 1 ] && echo -n '-'
    pwgen --capitalize --numerals --ambiguous 5 1 -1 | tr -d \\n
  done
}

main
